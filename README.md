# selecting-material-icons

Claude Code skill for picking [Material Design](https://fonts.google.com/icons) icon names.

Maps common concepts (users, products, orders, fields, statuses, actions) to semantically appropriate icon names like `shopping_cart`, `person`, `settings`.

Includes `scripts/icon_search.py` to query the official Material Icons metadata JSON.
